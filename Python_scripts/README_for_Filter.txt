Read processing using Filter_N_Adapter_Trim_Batchmode.py

# USAGE: python Programname.py [m] [-p]
# [m] enter minimum length after trimming
# [-p] option if running paired ends

This runs in batchmode within a folder that has the raw reads ending with '.fastq.gz' 

Filtered reads will be outputted in a '.fq' file.

